#include <stdio.h>
/**
 *main - Entry point
 *
 *Description : checking if the number stored in n is positive or negative
 *
 *Return: 0 (Success)
 */

int main(void)

{
	int n;

	if (n > 0)
	printf("n is positive: %d\n");
	else if (n < 0)
	printf("n is negative: %d\n");
	else (n = 0)
	printf("n equals 0: %d/n");
	return (0);
}
